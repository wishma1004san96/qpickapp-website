import type { TourPricingConfig } from "../types";

/** Configurable pricing copy — set numeric rates on packages when commercial sheet is ready. */
export const TOUR_PRICING: TourPricingConfig = {
  quoteLabel: "Pricing Available Upon Request",
  quoteHint:
    "Submit a tour request and our desk will confirm a written private-chauffeur quote for your dates and vehicle.",
};
